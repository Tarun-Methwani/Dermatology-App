<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ind.sihc.pdapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="RDermato"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutUsActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SliderActivity" />
        <activity android:name=".LoginActivityP" />
        <activity android:name=".SuccessActivityP" />
        <activity android:name=".ForgetPasswordActivityP" />
        <activity android:name=".RegisterActivityP" />
        <activity android:name=".DashboardActivityP" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".PharamcyActivityP"
            android:label="@string/title_activity_pharamcy_p" />
        <activity android:name=".FormActivityP" />
        <activity android:name=".LoginActivityD" />
        <activity android:name=".DashboardActivityD" />
        <activity android:name=".ForgetPasswordActivityD" />
        <activity android:name=".ListItemActivityD" />
        <activity android:name=".SettingActivityP" />
        <activity android:name=".DiagnosisActivityP" />
        <activity android:name=".PatientQueriesD" />
        <activity android:name=".QueryActivityD" />
        <activity android:name=".RecordActivityP" />
        <activity android:name=".ScannerActivity" />
        <activity android:name=".ResponseActivityP" />
        <activity android:name=".GraphActivity" />
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".QRCode" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".FAQ" />
        <activity android:name=".FAQpANS" />
        <activity android:name=".UrgentActivity" />
        <activity android:name=".NoturgentActivity" />
        <activity android:name=".RemediesNotUrgent" />
        <activity android:name=".QuestionSolutionActivity" />
        <activity android:name=".RemediesSolutionActivity" />
        <activity android:name=".BabyNotification" />
        <activity android:name=".RatingActivity" />
        <activity android:name=".GeographcalActivity" />
    </application>

</manifest>